# {{ ansible_managed }}
listen.port:
  - {{ mailrise_port }}
tls.mode:
  - none
configs:
  {{ mailrise_user }}@{{ mailrise_domain }}:
    urls:
      - pover://{{ pushover_user_key }}@{{ pushover_application_key | default(pushover_generic_application_key) }}
